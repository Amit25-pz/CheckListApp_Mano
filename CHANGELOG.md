# יומן גרסאות — יומן בדיקות ואחזקה בתא לחץ

כל השינויים המשמעותיים באפליקציה מתועדים כאן.
מספור הגרסאות: [Semantic Versioning](https://semver.org/lang/he/) — עיקרי.משני.תיקון

---

## [2.0.0] — 2026-07-03

### שינוי מהותי: צ'ק-ליסט לפי אתר (לפי האיפיון החדש)

**נוסף:**
- **5 תבניות צ'ק-ליסט לפי אתר** ([checklists.py](checklists.py)) — Arison, Segol, אלישע, חיל הים ותבנית כללית, מתומללות במדויק ממסמכי "יומן בדיקות ואחזקה בתא לחץ". בחירת האתר טוענת אוטומטית את הרשימה הנכונה.
- **מבנה היררכי** — כל סעיף ראשי (למשל "לוח חשמל ראשי") מכיל תתי-בדיקות מפורטות. כל תת-בדיקה מקבלת סטטוס משלה (תקין / לא תקין / הערה). בסך הכל עד ~110 תתי-בדיקות בתבנית המלאה.
- **שדות ערך משובצים** — "תאריך בדיקה אחרון", "שעות עבודה", "תאריך טיפול אחרון" בסעיפים הרלוונטיים.
- **שני דוחות PDF** ([pdf.py](pdf.py)) — דוח פנימי מלא + דוח ללקוח שמסתיר אוטומטית את השדות המסומנים "לא צריך להיות חשוף ללקוח" (מסומנים 🔒 בממשק).
- **שדה רבעון** — מחושב אוטומטית מהתאריך, ניתן לעריכה.
- **כפתור "סמן הכל תקין"** לכל סעיף — מילוי מהיר בשטח.
- **מונה חי** בראש המסך: סה"כ בדיקות / תקין / לא תקין / הערות.
- **כפתור "דוח חדש"** לאיפוס מלא של הטופס.
- **הורדת CSV** ישירות מהדפדפן (בנוסף לשמירה בתיקיית data/reports).
- **מספר גרסה** מוצג בסרגל הצד ונשמר בכל דוח.
- **מצב לילה 🌙** — מתג בסרגל הצד למעבר בין תצוגת יום לתצוגת לילה.
- **התראת "סעיפים עם תקלות"** בראש המסך — רואים מיד איפה יש בעיות בלי לפתוח סעיפים.
- **הגדרות אבטחה** ([.streamlit/config.toml](.streamlit/config.toml)) — השרת מאזין רק ל-localhost (לא חשוף לרשת), ללא שליחת סטטיסטיקות, הגבלת גודל העלאה.

**תוקן:**
- באג: בחירת סטטוס "הערה" ללא טקסט התאפסה ל"תקין" בריצה הבאה.
- באג: סימון "לא תקין" ראשון בסעיף סגר את הסעיף (כותרת דינמית שינתה את זהות ה-expander — הכותרות עכשיו קבועות).
- באג: שם קובץ הדוח "ננעל" לכל הסשן — שינוי אתר/מכונה אחרי שמירה ראשונה השאיר שם ישן.
- אבטחה: סניטציה אחידה של כל שמות הקבצים (כולל תמונות) — הגנה מ-path traversal בשמות שהוזנו חופשית.
- טיפול ב-PermissionError גם בשמירת תמונות (לא רק CSV).

**הוסר:**
- רשימת 38 הסעיפים השטוחה הגנרית (הוחלפה בתבניות לפי אתר).
- רשימת בתי החולים הפיקטיבית (איכילוב, הדסה...) — הוחלפה ברשימת האתרים האמיתית.
- אזור הצילום הנפרד בתחתית העמוד — המצלמה עברה לתוך כל סעיף.

**הערה:** אפליקציית המובייל (mobile/) עדיין בגרסה 1 — תעודכן בשלב נפרד.

---

## [1.0.0] — 2026-03 (תג git: v1.0.0)

הגרסה היציבה הראשונה:
- צ'ק-ליסט שטוח של 38 סעיפים ב-6 קטגוריות (זהה לכל האתרים).
- ממשק Streamlit בעברית (RTL) עם עיצוב לפי צבעי הלוגו.
- דוח PDF יחיד + שמירת CSV בקידוד utf-8-sig (תמיכה בעברית באקסל).
- צילום תמונות לפי קטגוריה.
- אפליקציית מובייל מקבילה (React Native + Expo) לאנדרואיד.
